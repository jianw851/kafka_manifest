apiVersion: v1
kind: Pod
metadata:
  name: zookeeper
spec:
  containers:
    - image: liveget/zookeeper_aarch64:0.0.1
      imagePullPolicy: Always
      name: zookeeper
  imagePullSecrets:
    - name: regcred
