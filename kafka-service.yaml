apiVersion: v1
kind: Service
metadata:
  labels:
    app: kafka
  name: kafka-service
spec:
  ports:
    - name: kafka-port
      port: 9092
      targetPort: 9092
      protocol: TCP
  selector:
    app: kafka
  type: LoadBalancer
