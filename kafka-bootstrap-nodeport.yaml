apiVersion: v1
kind: Service
metadata:
  labels:
    app: kafka
  name: bootstrap-external
spec:
  type: NodePort
  ports:
    - name: kafka-broker-nodeport
      port: 32094
      targetPort: 9094
      nodePort: 32094
      protocol: TCP
  selector:
    app: kafka
